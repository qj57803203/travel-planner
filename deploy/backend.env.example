# ── 必填 ──────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# ── 可选：高德地图（无则走纯文本交通）─────────────────
AMAP_WEB_KEY=

# ── 服务地址（Docker 内部，一般不用改）────────────────
# XHS_MCP_URL=http://xiaohongshu-mcp:18060/mcp
# CTRIPE_MCP_URL=1
# CHROME_DEBUG_URL=http://chrome:9222

# ── 超时配置（可选，一般默认即可）────────────────────
# XHS_MCP_TIMEOUT_S=40
# XHS_COLLECT_TIMEOUT_S=60
# AMAP_TIMEOUT_S=8
