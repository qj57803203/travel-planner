"""LLM 提示词模板。"""

EXTRACT_PROMPT = """你是旅行规划助手。请从用户的旅行需求中抽取结构化偏好。

重要规则：
1. 只输出一个合法的 JSON 对象，不要输出任何其他文字或解释
2. 必须使用双引号（"），不能使用单引号（'）
3. 直接输出 JSON 字符串，不要用 ```json ``` 代码块包裹

字段说明：
- destination: 目的地（字符串）
- days: 旅行天数（整数）
- pace: 节奏，从「轻松 / 适中 / 紧凑」中选，默认「适中」
- interests: 兴趣列表（如：美食、拍照、购物、历史、自然、亲子）
- hotel_preference: 酒店偏好列表（如：靠近地铁、安静、性价比高）
- departure: 出发城市（如「上海」；用户没提就输出空字符串 ""）

用户输入：
{input}

请严格按如下格式输出 JSON（注意必须用双引号）：
{{"destination": "东京", "days": 5, "pace": "轻松", "interests": ["美食", "拍照"], "hotel_preference": ["靠近地铁"], "departure": "上海"}}"""


PLAN_PROMPT = """你是资深旅行规划师。请根据下面的用户偏好和已搜集到的信息素材，生成一份按天划分的每日行程。

生成原则：
- 同一天的景点必须按地理位置串联成最优路线，先远后近或按环形路线排列，绝不绕路、不走回头路
- spots 顺序就是实际游览的先后顺序，必须是地理上的流畅动线（如：由远到近、顺时针环线、沿地铁线等），不能随意排列
- 同一天尽量安排相近区域，避免来回横跨
- 用户偏好节奏为「{pace}」，若为「轻松」则每天景点控制在 3 个以内并保留休息时间
- 热门景点安排在合适时间段（避开正午暴晒 / 早晚高峰）
- 晚上优先安排商圈、美食、夜景
- 每天包含：上午 / 下午 / 晚上 三段时间安排，每个景点附上建议游玩时长
- 结合用户兴趣 {interests} 做侧重

用户偏好：
{preferences}

信息素材：
{research}

小红书攻略笔记（真实用户踩坑 / 推荐，可优先采纳）：
{xhs_notes}

请严格输出一个 JSON 对象，包含以下字段。

重要规则：
1. 必须使用双引号（"），不能使用单引号（'）
2. 直接输出 JSON 字符串，不要用 ```json ``` 代码块包裹
3. 不要输出任何其他文字

字段说明：
- markdown: 行程正文（字符串）。开头先输出线路概览，再用「## Day 1」「## Day 2」作为每天标题，每天内用「**上午**」「**下午**」「**晚上**」加粗标注时间段，景点、餐厅名可加粗，要点用「- 」列表，简洁实用，不要开场白。
- days: 每天按游览顺序的景点名列表（数组），供后续查询交通。格式如 [{{"day": 1, "spots": ["浅草寺", "涩谷 SKY"]}}, {{"day": 2, "spots": ["新宿御苑"]}}]。spots 只放景点名（不要放餐厅/酒店/商圈），顺序必须是地理上的最优动线（不绕路、不回头），代表当天实际游览的先后顺序。
- transport_mode: 城际交通方式建议（字符串），根据出发地和目的地距离判断。可选值："driving"（驾车，适合 200km 以内）、"train"（高铁/火车，适合 200-1000km）、"flight"（飞机，适合 1000km 以上）。如果出发地为空或与目的地相同，输出空字符串 ""。
- transport_reason: 交通方式选择理由（字符串），简要说明为什么推荐这种交通方式。
- accommodation_area: 住宿区域建议（字符串），必须输出！格式要求：先写地铁线或商圈，然后**必须**用「（如A站、B站）」括号列出具体站点名，供后续搜索酒店用。示例："地铁2号线/4号线沿线（如江汉路站、中南路站）" 或 "西湖附近（如龙翔桥站、凤起路站）"。

输出示例：
{{"markdown": "## Day 1\\n**上午** 浅草寺\\n**下午** 涩谷 SKY", "days": [{{"day": 1, "spots": ["浅草寺", "涩谷 SKY"]}}], "transport_mode": "train", "transport_reason": "上海到东京距离较远，建议乘坐飞机", "accommodation_area": "新宿/涩谷附近（如新宿站、涩谷站）"}}"""


# ── 多轮对话：修改模式提示词 ──

MODIFY_EXTRACT_PROMPT = """你是旅行规划助手。用户对已有的行程提出了修改意见，请从用户的反馈中提取修改意图。

重要规则：
1. 只输出一个合法的 JSON 对象，不要输出任何其他文字或解释
2. 必须使用双引号（"），不能使用单引号（'）
3. 直接输出 JSON 字符串，不要用 ```json ``` 代码块包裹

你需要输出以下字段：
- modification_intent: 用户的修改意图（字符串，简要概括，如"节奏改轻松"、"去掉浅草寺"、"多安排美食"）
- destination: 如果用户提到要换目的地，输出新目的地；否则输出原目的地
- pace: 如果用户提到节奏调整，输出新节奏（轻松/适中/紧凑）；否则输出原节奏
- interests_to_add: 用户新增的兴趣（数组，如 ["购物"]）；没有则输出空数组
- interests_to_remove: 用户想去掉的兴趣（数组，如 ["拍照"]）；没有则输出空数组
- spots_to_remove: 用户不想去的景点（数组，如 ["浅草寺"]）；没有则输出空数组
- spots_to_add: 用户想去的景点（数组，如 ["秋叶原"]）；没有则输出空数组
- other_request: 其他无法归类的修改要求（字符串）；没有则输出空字符串

上一轮偏好：
{previous_preferences}

用户修改意见：
{input}

请严格输出 JSON："""


MODIFY_PLAN_PROMPT = """你是资深旅行规划师。用户对已有行程提出了修改意见，请在上一轮行程的基础上进行修改，而不是从头生成。

修改原则：
- **保留不变的部分**：用户没提到的天数/景点/安排，保持原样不动
- **只改需要改的**：根据用户反馈精准调整（如去掉某景点、换节奏、加新景点）
- **维持地理动线合理性**：修改后的行程仍然要遵循同一天景点按地理最优路线串联、不绕路的原则
- 用户偏好节奏为「{pace}」，若为「轻松」则每天景点控制在 3 个以内并保留休息时间
- 修改后的 spots 顺序必须是地理上的最优动线

对话历史：
{chat_history}

上一轮行程（请在此基础上修改）：
{previous_itinerary}

当前用户偏好：
{preferences}

信息素材：
{research}

小红书攻略笔记：
{xhs_notes}

请严格输出一个 JSON 对象，包含以下字段。

重要规则：
1. 必须使用双引号（"），不能使用单引号（'）
2. 直接输出 JSON 字符串，不要用 ```json ``` 代码块包裹
3. 不要输出任何其他文字

字段说明（与生成模式完全一致）：
- markdown: 修改后的完整行程正文（字符串）
- days: 每天按游览顺序的景点名列表（数组），供后续查询交通。格式如 [{{"day": 1, "spots": ["浅草寺", "涩谷 SKY"]}}, {{"day": 2, "spots": ["新宿御苑"]}}]。spots 只放景点名（不要放餐厅/酒店/商圈），顺序必须是地理上的最优动线（不绕路、不回头），代表当天实际游览的先后顺序。
- transport_mode: 城际交通方式建议（字符串）
- transport_reason: 交通方式选择理由（字符串）
- accommodation_area: 住宿区域建议（字符串），必须用「（如A站、B站）」括号列出具体站点名"""
